// 01:
var num = 9;
let myName = "Abid Khokhar";
const myStatus = true;
const myData = {roll:109, profession: "Student"}
let favColors = ["blue", "green"]
// Priniting
console.log(num, typeof(num)) // number
console.log(myName, typeof(myName)) // string
console.log(myStatus, typeof(myStatus)) // boolean
console.log(myData, typeof(myData)) // object
console.log(favColors, typeof(favColors)) // object



// 02 : Difference b/w Var, Let & Const

// var can be redeclared & reassigned
// let can't be redeclared but can be reassigned
// const can't be redeclared & can't be reassigned too.
